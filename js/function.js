const sum=(a,b )=>{console.log("hello"); // a,b is aparameters it will perform task //arrow function
//const a="20";
//const b=30;
const c="10";
const d=a+b;            //this will perform string concatenation because both a and b are strings
const e=b+c;            //this will perform addition because both b and c are numbers
console.log(d);
console.log(e);            //string concatenation // it is powerfull
console.log(a+b);}        //this will perform string concatenation because both a and b are strings
sum(10,10);      //10,10 are arguments //it sum all the value inside the function
